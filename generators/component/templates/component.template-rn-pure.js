import React from 'react';
import {
  View,
} from 'react-native';
<%if (STYLESHEET === 'Y') { -%>import styles from './stylesheet';<% } -%>


export default ({}) => {
    return (
        <View></View>
    );
}
